[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Header PWA: permette al service worker di controllare tutta l'app
[[headers]]
  for = "/sw.js"
  [headers.values]
    Service-Worker-Allowed = "/"
